[project]
name = "vagrant-story-launcher"
version = "0.1.0"
description = "Locked bootstrap environment for the Vagrant Story port"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
