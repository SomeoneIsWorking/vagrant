#pragma once

class Core;

namespace vagrant::cd {

// Native handler body for the measured blocking DsControlB boundary. The future title adapter owns
// image-scoped registration with psxport's dynarec; this module owns only Vagrant's semantics.
void handleDsControlB(Core &core);

} // namespace vagrant::cd
